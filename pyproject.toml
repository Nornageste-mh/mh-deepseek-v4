[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "mh-deepseek-agent"
version = "4.0.0"
description = "通用专家型智能体 — 跨平台 AI Agent"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
keywords = ["ai", "agent", "deepseek", "android", "automation"]

[project.urls]
Homepage = "https://github.com/yourusername/mh-deepseek-agent"
